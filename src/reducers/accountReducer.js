const Account=(state={name:"peyman",family:"ahmadian"},action)=>{
    switch (action.type) {
        default:
            return{
                ...state
            }
    }
}
export default Account;